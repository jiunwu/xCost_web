<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Your Saved Items</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <style>
    /* Base Styles */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: "Inter", sans-serif;
      color: white;
      background-color: black;
      min-height: 100vh;
      overflow-x: hidden;
    }

    .container {
      width: 100%;
      max-width: 1200px;
      margin: 0 auto;
      padding: 24px;
    }

    .headline {
      font-size: 36px;
      font-weight: 700;
      margin-bottom: 24px;
    }

    .saved-items-list {
      list-style: none;
      padding: 0;
    }

    .saved-item {
      font-size: 18px;
      margin-bottom: 16px;
      padding: 12px;
      background: rgba(255, 255, 255, 0.1);
      border-radius: 8px;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1 class="headline">Your Saved Items</h1>
    <ul class="saved-items-list">
      <% savedItems.forEach(function(item) { %>
        <li class="saved-item"><%= item.name %> - <%= item.description %></li>
      <% }); %>
    </ul>
  </div>
</body>
</html>